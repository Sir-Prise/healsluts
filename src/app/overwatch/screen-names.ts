export type OverwatchScreenName = 'menues' | 'alertPopup' | 'loadingMap' | 'heroSelection' | 'matchAlive' | 'matchNoPrimary' | 'matchDead' | 'killcam' | 'deadSpectating' |
    'potgSpectating' | 'scoreBoard' | 'interactionMenu' | 'black' | 'undefined';
/* TODO Screens:
 * scoreScreen		(e.g. 1:0)
 * matchResult		("e.g. VICTORY")
 * victoryPoses
 * potgIntro
 * cards
 * endorsementSelector
 * stats			(stats shown after the match)
 */
